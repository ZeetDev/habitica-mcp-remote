services:
  - type: web
    name: habitica-mcp
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: HABITICA_USER_ID
        sync: false
      - key: HABITICA_API_TOKEN
        sync: false
      - key: NODE_ENV
        value: production
